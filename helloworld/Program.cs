using System;

class Program
{
    static void Main(string[] args)
    {
        // Declare a variable to store the user's name
        string name = "sri";
        
        // Use string interpolation to concatenate the greeting and the name
        // This is a more readable and efficient way to combine strings
        Console.WriteLine($"Hello, {name}!");
    }
}
